<% provide(:title, 'Новости') %>
  <h1>Фондовый рынок</h1>
<div class="row">
	<div class="span12">
		<%= will_paginate %>
			<ol class="microposts">
      	<%= render @bands %>
			</ol>
		<%= will_paginate %>
	</div>
</div>
